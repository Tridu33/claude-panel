# Tmux 会话日志终端功能说明

## 功能概述

在 Claude Panel 中新增了 tmux 会话日志终端显示功能,可以实时查看选定 tmux 会话的输出日志。

## 主要特性

### 1. 删除旧组件
- ❌ 删除了"流水灯 · Task 运行状态"
- ❌ 删除了"液晶屏 · 信息 / 动图"
- ✅ 页面更加简洁,聚焦核心功能

### 2. 新增终端日志显示

#### 显示内容
- 根据当前选择的 tmux 会话,显示该会话的最新输出日志
- 默认显示最后 200 行
- 支持自定义显示行数(10-1000 行)

#### 自动刷新
- 点击"自动"按钮开启/关闭自动刷新
- 自动刷新间隔: 5 秒
- 开启时按钮显示为绿色并显示"5s"

#### 手动刷新
- 点击"刷新"按钮手动加载最新日志
- 加载时显示旋转动画
- 自动滚动到最新内容底部

## 使用方式

### 1. 选择会话
从顶部下拉菜单选择一个活跃的 tmux 会话

### 2. 查看日志
- 终端区域自动显示该会话的最新日志
- 日志内容使用等宽字体显示,保持格式

### 3. 调整行数
在"行数"输入框中输入要显示的行数(10-1000)
- 修改后自动加载
- 按 Enter 键确认

### 4. 自动刷新
- 点击"自动"按钮开启自动刷新(每 5 秒)
- 再次点击关闭自动刷新
- 按钮状态变化:
  - 关闭: 灰色"自动"
  - 开启: 绿色"5s"

### 5. 手动刷新
- 点击"刷新"按钮立即加载最新日志
- 加载过程中按钮显示旋转动画

## 界面布局

```
┌─────────────────────────────────────────────────┐
│ 🔗 会话日志: codes.mac.Users    [行数: 200]     │
│                                [自动/5s] [刷新] │
├─────────────────────────────────────────────────┤
│                                                 │
│  终端日志内容区域                                 │
│  - 等宽字体显示                                  │
│  - 自动滚动到底部                                │
│  - 支持滚动查看历史                              │
│  - 高度: 500px                                  │
│                                                 │
│                                                 │
└─────────────────────────────────────────────────┘
```

## API 接口

### 获取会话日志

```bash
GET /api/tmux/logs?session={session_name}&lines={line_count}

参数:
- session: 会话名称(必填)
- lines: 获取最后多少行(可选,默认 200)

响应:
{
  "success": true,
  "session": "codes.mac.Users",
  "logs": "日志内容...",
  "lines": 200
}
```

### 错误响应

```json
// 会话不存在
{
  "error": "会话不存在: codes.mac.Users"
}

// 获取失败
{
  "error": "获取日志失败: 错误信息"
}
```

## 技术实现

### 后端 (Python)
- 使用 `tmux capture-pane` 命令捕获会话输出
- `-p` 参数: 输出到 stdout
- `-S -{lines}` 参数: 从倒数第 N 行开始捕获
- 超时设置: 5 秒

### 前端 (Vue 3)
- 使用 `<pre>` 标签保持日志格式
- 自定义滚动条样式
- 自动滚动到最新内容
- 定时器实现自动刷新
- 加载状态动画

## 样式特点

### 终端风格
- 深色背景 (#0f172a)
- 等宽字体 (Menlo, Monaco, Courier New)
- 自定义滚动条
- 圆角边框

### 交互反馈
- 按钮悬停效果
- 加载旋转动画
- 自动刷新状态指示
- 禁用状态样式

## 注意事项

1. **会话必须存在**: 只能查看已存在的 tmux 会话
2. **权限要求**: 运行服务的用户需要有权限访问 tmux 会话
3. **性能考虑**: 
   - 行数越多,加载越慢
   - 建议不超过 1000 行
   - 自动刷新会持续消耗资源
4. **实时性**: 
   - 自动刷新有 5 秒延迟
   - 需要最新内容请手动刷新

## 常见问题

### Q1: 显示"会话不存在"
**原因**: 会话已被关闭或从未创建

**解决**: 
```bash
# 查看所有活跃会话
tmux list-sessions

# 创建新会话
tmux new-session -d -s mysession
```

### Q2: 日志内容为空
**原因**: 会话刚创建,还没有输出

**解决**: 在会话中执行一些命令后刷新

### Q3: 自动刷新不工作
**原因**: 可能是浏览器限制或网络问题

**解决**:
1. 检查浏览器控制台是否有错误
2. 尝试手动刷新
3. 重新开启自动刷新

### Q4: 日志显示不完整
**原因**: 行数设置过小

**解决**: 增加行数设置(如改为 500 或 1000)

## 使用场景

### 场景 1: 监控 Claude 运行
1. 选择 Claude 运行的 tmux 会话
2. 开启自动刷新
3. 实时监控 Claude 的输出

### 场景 2: 查看构建日志
1. 选择运行构建任务的会话
2. 设置较大的行数(如 500)
3. 查看完整的构建输出

### 场景 3: 调试问题
1. 选择出错的会话
2. 手动刷新获取最新日志
3. 分析错误信息

## 未来改进

- [ ] 支持日志搜索/过滤
- [ ] 添加日志下载功能
- [ ] 支持多个会话同时监控
- [ ] 添加日志高亮(错误/警告)
- [ ] 支持暂停/恢复自动滚动
- [ ] 添加时间戳显示
